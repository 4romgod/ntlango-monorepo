{
  "name": "ntlango-cdk",
  "description": "AWS CDK Infrastructure for Dyondo muliti-user blogging platform",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "main": "dist/lib/app.js",
  "scripts": {
    "build": "npm run clean && echo 'Transpilling typescript...' && tsc",
    "clean": "echo 'Cleaning up...' && rm -rf dist && rm -rf cdk.out",
    "watch": "tsc -w",
    "test": "jest --verbose",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/prettier": "2.6.0",
    "aws-cdk": "2.7.0",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "aws-cdk-lib": "2.7.0",
    "constructs": "^10.0.0"
  }
}
