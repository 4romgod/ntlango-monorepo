{
  "name": "@ntlango/commons",
  "version": "1.0.0",
  "description": "This repository contains common source code for ntlango",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "npm-run-all lint:check build:ts",
    "build:ts": "echo '\nTranspilling ts source code...' && tsc --build --pretty --verbose && echo 'All done with the build!'",
    "lint:check": "eslint \"lib/**/*.{ts,tsx}\"",
    "lint:fix": "eslint \"lib/**/*.{ts,tsx}\" --fix",
    "typecheck": "tsc --noEmit --pretty"
  },
  "dependencies": {
    "@typegoose/typegoose": "^12.4.0",
    "date-fns": "^3.6.0",
    "graphql": "^16.8.1",
    "graphql-type-json": "^0.3.2",
    "mongoose": "^8.4.1",
    "reflect-metadata": "^0.2.2",
    "type-graphql": "^2.0.0-rc.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint-plugin-unused-imports": "^4.3.0"
  }
}
