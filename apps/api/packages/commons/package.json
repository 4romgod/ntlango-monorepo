{
  "name": "@ntlango/commons",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:ts",
    "build:ts": "echo '\nTranspilling ts Commons code...' && tsc --build --pretty --verbose && echo 'All done with the build!'"
  }
}
